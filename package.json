{
  "name": "cc-plugin-codex",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "description": "Use Claude Code from Codex to review code or delegate tasks.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check plugins/cc/scripts/claude-companion.mjs && node --check plugins/cc/scripts/lib/*.mjs && node --test tests/*.test.mjs"
  }
}
